class SpotifyMusic {
  final String title, artist, image, audioUrl;
  SpotifyMusic(
      {required this.title,
      required this.artist,
      required this.image,
      required this.audioUrl});
}
